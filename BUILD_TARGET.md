# Build target

Remote 3 current public upstream validation target.

The device reports Core 0.77.8-non-git and UI 0.79.0. Public remote-ui source currently exposes v0.73.5 in its changelog; the build must pin the latest public main commit and must not claim to be identical to the installed UI.
